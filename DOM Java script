var x = prompt("Name");
document.getElementById('one').innerHTML = x;

//DOM

<p id="one"></p>
// html ID


//function and DOM JS DOM
function showName(){
	var userName = document.getElementById('userName').value;
  document.getElementById('one').innerHTML="Hello" + userName;
}
//HTML Input and button and id 
<Input type="text" id="userName"></Input> 
<button onclick="showName()">Show my name</button>
<p id="one"></p>


//Css change font size
#one {
  font-size: 200%;
}


//killing people

<p class="stem">Duane</p>
<p class="stem">Bob</p>
<p class="stem">Brandon</p>
<p class="stem">Dod</p>
<p class="stem">Carlos</p>
<p class="stem">Ace</p>
<button onclick="deathNote()">Random death</button>

function deathNote() {
var death = Math.floor(Math.random() * 6);
 	document.getElementsByClassName('stem')[death].innerHTML = "xxxxxxxxxxx";
	
	
//with html above this is to add pictures	
	
	function deathNote() {
var death = Math.floor(Math.random() * 6);
 	document.getElementsByClassName('stem')[death].innerHTML = "<img src='https://www.singularityweblog.com/wp-content/uploads/2016/02/Death.jpg' width= '10%'>";
}
}






//create an element with text

<p> Click the button to create a P element with some text.</p>
<button onclick="myFunction()">Try it
</button>




function myFunction() {
var para = document.createElement("P");
var t = document.createTextNode("This is a paragraph");
para.appendChild(t);
document.body.appendChild(para);
}





//adding two variables Math.power, Math.sqrt
//watch out for quotes from google docs might be a little slanted


A:
<input type="number" id='a'> 
<br>
B:
<input type= "number" id="b">
<br>
C:
<p id="c"></p>
<button onclick="showC()">Show my answer</button>




function showC() {

var a = document.getElementById('a').value;
var b = document.getElementById('b').value;
document.getElementById('c').innerHTML = Math.sqrt(Math.pow(a,2) + Math.pow(b,2));
}
